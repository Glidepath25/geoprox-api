version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt

run:
  command: python3 start.py
  network:
    port: 8000

healthcheck:
  path: /healthz
  interval: 5
  timeout: 2
  healthy_threshold: 1
  unhealthy_threshold: 3
