version: 1.0
runtime: python3
build:
  commands: "python3 -m pip install --upgrade pip setuptools wheel && python3 -m pip install --no-cache-dir -r requirements.txt"
run:
  command: "gunicorn app.main:app --bind 0.0.0.0:8000 -w 2 -k uvicorn.workers.UvicornWorker"
network:
  port: 8000
